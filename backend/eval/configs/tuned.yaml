# Fitted AttentionConfig overrides. Regenerated by:
#   eval/sweep.py              (per-frame ground-truth labels)
#   eval/tune_from_feedback.py (teacher 5-minute segment ratings)
#
# Empty until a fit has been run. Point config.ATTENTION_CONFIG_PATH at this
# file (ATTENTION_CONFIG_PATH=eval/configs/tuned.yaml) to load it over the
# defaults in app/config.py at startup.
#
# Example of what a run writes:
#   yaw_enter_deg: 22.0
#   yaw_exit_deg: 32.0
#   desk_work_pitch_deg: 15.0
#   desk_work_weight: 0.5
#   on_task_floor: 0.5
